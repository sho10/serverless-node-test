// @ts-ignore
import config from "./config.ts"

// export async function connectToDB() {
//     process.env.DATABASE_URL = `postgresql://${config.DATABASE_USER}:${config.DATABASE_PASSWORD}@${config.DATABASE_PORT}/${config.DATABASE_NAME}?schema=public`
// }